/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["127.0.0.1", "localhost", "https://anamrinarecruitment.com/", "https://admin.anamrinarecruitment.com/"],
    }
};

export default nextConfig;
